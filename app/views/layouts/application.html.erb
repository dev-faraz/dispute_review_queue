<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
  <head>
    <title>Dispute Review Queue</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <link href="https://rsms.me/inter/inter.css" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <% if user_signed_in? %>
      <div class="bg-indigo-800 text-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <div class="flex items-center space-x-8">
              <%= link_to "Dispute Queue", root_path, class: "text-xl font-bold" %>
              <nav class="flex space-x-6">
                <%= link_to "Reports", reports_daily_volume_path, class: "hover:text-indigo-200" %>
              </nav>
            </div>
            <div class="flex items-center space-x-4 text-sm">
              <span><%= current_user.email %></span>
              <span class="text-indigo-200">(<%= current_user.role.humanize %>)</span>
              <span class="text-indigo-300">· <%= current_user.time_zone %></span>
              <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "hover:text-indigo-200" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <% if notice || alert %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-6">
        <% if notice %>
          <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg"><%= notice %></div>
        <% end %>
        <% if alert %>
          <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg"><%= alert %></div>
        <% end %>
      </div>
    <% end %>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
  </body>
</html>